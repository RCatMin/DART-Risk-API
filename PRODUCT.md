# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

React + Vite (SPA). Phase 7에서 만든 `openapi.yaml`을 백엔드 API 계약으로 소비하는 순수 클라이언트 사이드 앱. CLAUDE.md에 이미 확정된 결정 (Phase 8 스코프).

## Users

- **1차 (설계 우선순위 — 확정)**: 워치리스트에 등록된 상장사를 모니터링하는 분석가/투자자. 특정 종목의 최근 공시에 감사의견 비적정·횡령·배임·소송·관리종목 지정 같은 리스크가 있는지 빠르게 훑어보고 싶어함. 대시보드는 이 사용자의 실사용 시나리오(스캔성, 정보 밀도, 반복 사용 편의성)를 기준으로 설계한다.
- **2차 (배경 맥락)**: 이 프로젝트를 포트폴리오로 보는 채용 담당자/개발자. 디자인 우선순위를 좌우하지는 않지만, 완성도 있는 디테일은 자연히 이 사용자에게도 신뢰를 준다.

## Product Purpose

워치리스트 종목(50~100개)의 사업보고서·감사보고서에서 자동 탐지된 리스크 플래그를 **조회 전용**으로 보여주는 대시보드. 백엔드가 이미 수집·분류를 끝낸 데이터를, 투자 자문으로 오인되지 않게 신뢰성 있게 노출하는 것이 성공 기준.

## Positioning

전 상장사를 실시간 감시하거나 XBRL로 재무제표를 정밀 분석하는 도구들과 달리, 이 제품은 "좁고 확실한" 범위(워치리스트 + 사업/감사보고서 2종 + 고정 5개 리스크 유형)만 다뤄서 룰 기반 분류의 정확도를 확보하는 데 집중한다.

## Operating Context

- 데이터는 사람이 아니라 BullMQ 워커 파이프라인(수집 → 추출 → 분류)이 채운다. 대시보드는 쓰기 경로가 없는 순수 조회 클라이언트.
- 백엔드 API는 `x-api-key` 헤더 인증이 필요하고 Redis 캐시(TTL 30초)가 적용돼 있음 — 같은 화면을 반복 요청해도 서버 부하가 낮다.
- API 계약은 `openapi.yaml`(Phase 7 산출물) — 엔드포인트: `GET /api/companies`, `GET /api/companies/:corpCode/disclosures`, `GET /api/disclosures/:id`, `GET /api/risk-flags`.

## Capabilities and Constraints

- 조회 가능한 리스크 유형은 정확히 5개로 고정: 감사의견 비적정 / 횡령·배임 / 소송 / 관리종목 지정 / 해당없음. 다른 유형 추가는 스코프 밖.
- 대상 문서는 사업보고서·감사보고서 2종만 — 다른 보고서 타입은 표시하지 않는다.
- 실시간 웹소켓 알림 없음 (v1은 REST 조회 + 선택적 Slack 알림만).
- 모든 화면에 "투자 자문이 아니라 참고용 정보" disclaimer가 항상 노출되어야 함 — 리스크 정보 근처에서 눈에 띄게 표시.
- 매수/매도 추천으로 읽힐 수 있는 문구·UI(색상으로 "사라/팔아라" 뉘앙스를 주는 것 포함)는 금지.
- 인증: 프론트엔드가 API 키를 어떻게 보관·전달할지는 **의도적으로 결정 보류** — Phase 8 실제 구현 시점에 다시 논의 (빌드타임 `.env` 주입 vs 프록시/BFF 경유 등). 백엔드는 헤더 기반 API 키만 있고 사용자 로그인 개념이 없음.

## Brand Commitments

없음 — 신규 프로젝트, 기존 브랜드·로고·보이스 자산 없음.

## Evidence on Hand

- 실제 시드 데이터 존재: 워치리스트 79개 기업(예: 삼성전자, corp_code `00126380`), 공시 249건, 리스크플래그 679건 — 화면 설계 시 이 실데이터를 그대로 사용 가능.
- API 계약 문서: `openapi.yaml`.
- 지금까지의 아키텍처·캐시 전략 문서: `DESIGN.md`.
- Phase별 변경 로그: `PHASE1_LOG.md` ~ `PHASE7_LOG.md`.

## Product Principles

1. 조회 전용 — 대시보드에서 데이터를 쓰거나 수정하지 않는다.
2. 스코프를 넓히지 않는다 — 5개 리스크 유형, 2개 보고서 타입, 워치리스트 종목 외에는 표시하지 않는다.
3. disclaimer는 타협 불가 — 어떤 화면에서도 투자 자문처럼 보이는 문구·UI를 만들지 않는다.
4. 백엔드가 이미 낸 신뢰도(`confidence`)와 원문 근거(`source_snippet`)를 숨기지 않고 보여줘서, 룰 기반 분류의 한계를 사용자가 스스로 판단할 수 있게 한다.

## Accessibility & Inclusion

확인 결과 별도의 접근성 기준(WCAG 등)을 요구하지 않기로 확정 — 포트폴리오 스코프. 시맨틱 HTML 등 기본적인 수준은 자연스럽게 지키되, 별도 감사·강제는 하지 않는다.
